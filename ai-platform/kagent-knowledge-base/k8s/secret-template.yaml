apiVersion: v1
kind: Secret
metadata:
  name: platform-kb-openai
  labels:
    app.kubernetes.io/name: platform-kb
    app.kubernetes.io/part-of: kagent
type: Opaque
stringData:
  # This is an example only and is not included in kustomization.yaml.
  # Create it separately:
  # kubectl -n kagent create secret generic platform-kb-openai \
  #   --from-literal=OPENAI_API_KEY="<key>"
  # Add --from-literal=AZURE_OPENAI_KEY="<key>" when EMBEDDING_PROVIDER=azure.
  OPENAI_API_KEY: "replace-me"
  AZURE_OPENAI_KEY: "replace-me"
