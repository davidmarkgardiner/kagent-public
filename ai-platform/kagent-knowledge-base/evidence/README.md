# Evidence

Run:

```bash
cd ai-platform/kagent-knowledge-base
./scripts/capture-evidence.sh
```

Expected outputs:

- `EVIDENCE.md`
- `rendered-platform-kb.yaml`
- optional `querydoc-smoke-container.log` when the local querydoc smoke test is run

