# Prompt: Prove HITL Remediation

```text
Prove a non-read-only remediation step pauses for human approval, records the
decision, resumes only after approval, and verifies the action afterwards.

Use approval route: {{APPROVAL_CHANNEL}}.
```
