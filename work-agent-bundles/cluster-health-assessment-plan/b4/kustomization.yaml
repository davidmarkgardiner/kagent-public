apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - workload.yaml
configMapGenerator:
  - name: cluster-health-summary-reconciler-script
    namespace: agent-health-system
    files:
      - reconciler.py
generatorOptions:
  labels:
    app.kubernetes.io/name: cluster-health-summary-reconciler
