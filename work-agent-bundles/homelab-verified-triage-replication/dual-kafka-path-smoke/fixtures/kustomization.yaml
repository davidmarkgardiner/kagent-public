apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This namespace is fixed because the dual-path Alloy source watches only
# dual-kafka-smoke. Change the source config as well if work needs another one.
namespace: dual-kafka-smoke
images:
- name: dual-kafka-smoke-image
  newName: busybox:1.36

resources:
- dual-kafka-marker.yaml
