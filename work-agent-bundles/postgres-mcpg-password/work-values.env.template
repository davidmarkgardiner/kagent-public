# Copy to work-values.env, replace every {{PLACEHOLDER}}, and do not commit it.
dataMcpNamespace={{DATA_MCP_NAMESPACE}}
agentGatewayNamespace={{AGENTGATEWAY_NAMESPACE}}
agentGatewayName={{AGENTGATEWAY_NAME}}
agentGatewayService={{AGENTGATEWAY_SERVICE}}
postgresMcpServiceHost=postgres-inventory-mcp.{{DATA_MCP_NAMESPACE}}.svc.cluster.local
postgresMcpGatewayUrl=http://{{AGENTGATEWAY_SERVICE}}.{{AGENTGATEWAY_NAMESPACE}}.svc.cluster.local/mcp/postgres-inventory
kagentNamespace={{KAGENT_NAMESPACE}}
kagentModelConfig={{KAGENT_MODEL_CONFIG}}
mcpgImage={{INTERNAL_REGISTRY}}/third-party/mcpg@sha256:{{IMAGE_DIGEST}}
postgresMcpSecretName={{POSTGRES_MCP_SECRET_NAME}}
