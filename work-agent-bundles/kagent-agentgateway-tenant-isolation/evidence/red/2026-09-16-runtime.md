# Red runtime receipt, 2026-09-16

The final integrated verification run started at `2026-09-16T18:44:09Z` on Kubernetes `v1.32.2` and is summarized in `2026-09-16-summary.tsv`. Its raw inventory and response bodies were written to an operating-system temporary directory outside the public repository.

Observed deployed releases:

| Release | Namespace | Chart | State |
| --- | --- | --- | --- |
| `agentgateway-crds` | `agentgateway-system` | `agentgateway-crds-v1.5.0` | deployed |
| `kagent-crds` | `kagent` | `kagent-crds-0.10.1` | deployed |
| `tenant-agentgateway` | `tenant-gateway-system` | `agentgateway-v1.5.0` | deployed |
| `tenant-kagent` | `tenant-kagent-system` | `kagent-0.10.1` | deployed |
| `kagent` | `kagent` | `kagent-0.10.0-beta7` | deployed historical Substrate owner |
| `substrate` | `ate-system` | `substrate-0.0.8` | deployed historical runtime |

Observed canary images were `cr.agentgateway.dev/controller:v1.5.0`, `cr.agentgateway.dev/agentgateway:v1.5.0`, and `ghcr.io/kagent-dev/kagent/controller:0.10.1`. The canary agentgateway controller used `agentgateway.dev/tenant-isolation`, `tenant-agentgateway`, namespace discovery selectors, and `AGW_BACKEND_REF_GRANT_MODE=route-and-policy`. Kagent used `AUTH_MODE=trusted-proxy` behind the network-restricted authenticated Gateway path.

The in-cluster event-source path returned a completed A2A task containing `INC-1001` and increased the event MCP call counter. The chat path returned a completed A2A task containing `REL-204` and increased the chat MCP call counter. The fifth listener returned a completed `machinist-security-review` SandboxAgent task containing `TRUST_BOUNDARIES` and `SECURITY_VERDICT` under context `context-verify-substrate-20260916T184409Z`. The Substrate API then recorded a successful `SuspendActor`, actor status 4, and a new external snapshot prefix. Before the call, the specialist was Accepted and Ready, exactly one ActorTemplate was Ready with a golden snapshot, and its gVisor WorkerPool was 3/3.

Wrong, expired, cross-purpose, cross-audience, and rogue-claim tokens were rejected without changing the tenant MCP backend request counter. The Substrate listener separately returned 401 for no token, 401 for a valid chat token, and 403 for a correctly signed Substrate-audience token with rogue claims. A known but disallowed MCP tool returned 400 without changing either the backend request counter or forbidden-tool counter. Direct team-Service, dedicated kagent-controller, and shared SandboxAgent endpoint access from the rogue namespace timed out or was refused. A team-admin attempt to put the reserved kagent label only in a Deployment Pod template was rejected by the intended admission rule. A BYO Agent was also rejected, keeping the tenant controller lane declarative-only. RBAC, cross-reference admission, Pod Security, and missing-ReferenceGrant tests were denied.

The before and after preflight inventories each contained the same 13 pre-existing, non-canary HTTPRoutes with an Accepted true condition. This is a scoped regression check for the shared CRD upgrade, not proof that every pre-existing application behavior remained unchanged.

The Entra JWKS backend and five placeholder-substituted AgentgatewayPolicies, including `substrate-a2a-auth`, passed server-side dry-run against the installed agentgateway v1.5 CRDs. The exact sanitized procedure and expected route-overlay rejection are retained in `2026-09-16-entra-schema-dry-run.md`. The Entra route overlay intentionally did not pass on red because its Gateway API v1.4 CRDs do not contain the v1.6 experimental CORS field.

The red run proves the disposable JWT profile, not the Entra profile. Red has no workplace Entra tenant, public hostname, certificate, or workload-token broker. The AKS overlay remains promotion-gated on Gateway API v1.6.2 installation, live discovery, PKCE, app-role denial, and service-token refresh across expiry. The cluster's pre-existing shared controllers and Substrate release were not upgraded, but the shared cluster-scoped kagent and agentgateway CRD Helm releases were upgraded in place. The older shared agentgateway controller adds status noise to policies; the receipt evaluates the dedicated controller's status ancestor only.

This runtime is intentionally not the workplace version target. Red's live WorkerPool uses `ateom-gvisor:v0.0.8`, and its live ActorTemplate uses the historical golang-adk digest beginning `sha256:86031a`. The lift-and-shift target is the independently canaried kagent `0.10.1` plus Substrate `0.0.9` pair with complete digests in `platform/substrate/target-platform.env`. Credential-free readiness of that target does not prove provider-backed A2A or AKS gVisor behavior.

On 2026-09-16, `scripts/verify-substrate-target.sh` passed its version/digest consistency, 16-row lock, no-tools, YAML, and red server-dry-run checks. `scripts/verify-substrate-images.sh` then resolved all 16 immutable source image digests successfully. This proves source artifact availability and lock consistency, not that the images have been mirrored or admitted in a workplace registry.
