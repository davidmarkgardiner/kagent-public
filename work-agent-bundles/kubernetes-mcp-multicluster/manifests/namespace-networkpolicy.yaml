apiVersion: v1
kind: Namespace
metadata:
  name: "HOST_NAMESPACE_REPLACE"
  labels:
    app.kubernetes.io/part-of: kubernetes-mcp-fleet-poc
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: "HOST_NAMESPACE_REPLACE"
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: kubernetes-mcp-fleet-allow
  namespace: "HOST_NAMESPACE_REPLACE"
spec:
  podSelector:
    matchLabels:
      kubernetes-mcp-fleet/access: server
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: "KAGENT_NAMESPACE_REPLACE"
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: "AGENTGATEWAY_NAMESPACE_REPLACE"
        - podSelector:
            matchLabels:
              kubernetes-mcp-fleet/access: smoke-client
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Environment-neutral initial rule. Production overlays should constrain
    # destination CIDRs as well as these Kubernetes API ports.
    - ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 6443
